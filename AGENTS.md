# Codex Cloud Agent Context

## Git & Workflow Rules
- Always create feature branches off the `develop` branch.
- Always open Pull Requests targeting the `develop` branch. Never target `main`.
- Always open full Pull Requests, not drafts. 
